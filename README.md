# linkedin-oauth2-example
An example of getting access token from LinkedIn using its OAuth2 process


```
npm install
CLIENT_SECRET=<fill this with right value> node .
```

Then visit http://localhost:4000
